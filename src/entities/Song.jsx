export default class Song {
    constructor(id, title, artist, album, artworkUrl) {
      this.id = id;
      this.title = title;
      this.artist = artist;
      this.album = album;
      this.artworkUrl = artworkUrl;
    }
  }